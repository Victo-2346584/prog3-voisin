#pragma once
#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>
#include <numeric>
using namespace std;

float calculerMoyenne(vector<float>);
float calculerMediane(vector<float>);
float calculerEcartType(vector<float>);
float calculerDistanceEuclidienne(vector<float>, vector<float>);
float calculerDistanceManhattan(vector<float>, vector<float>);